import React from 'react'; // eslint-disable-line no-unused-vars
import HeaderBase from '../../components/Header';

const Header = (props) => (
  <HeaderBase {...props} title='La lógica transmedia, cómo llegar en distintos formatos a
  diferentes públicos' />
);

export default Header;
