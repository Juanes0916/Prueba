import React from 'react'; // eslint-disable-line no-unused-vars
import HeaderBase from '../../components/Header';

const Header = (props) => (
  <HeaderBase {...props} title='Innovación en negocios del sector creativo y cultural' />
);

export default Header;
